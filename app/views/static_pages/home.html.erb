<h1>Welcome to Carson Auto</h1>
<p>
	
	<% Car.all.each do |car| %>
	<div>
  		<%= image_tag car.image_desc_url(:thumb).to_s %><br>
  		<%= car.make %><br>
  		<%= car.year %><br>
  		<%= car.price %><br>
  	</div>
	<% end %>

</p>
